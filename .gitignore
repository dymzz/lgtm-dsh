# ---------------------------------------------------------------------------
# 依赖：绝不能提交
# ---------------------------------------------------------------------------
# node_modules 下 @deepseek-ai 和 @stardeckai/lgtm 都是指向**本机 DSH profile** 的
# junction。入库等于把两条指向我个人机器的链接和几万行依赖写进历史。
node_modules/

# ---------------------------------------------------------------------------
# 本地文档：不入库
# ---------------------------------------------------------------------------
# 原始设计前置文档，属于本机工作材料（不带斜杠，任意层级都匹配）。
pre_design.md

# ---------------------------------------------------------------------------
# 打包产物
# ---------------------------------------------------------------------------
# npm pack / dsh plugin add 的 tarball，随时可重建。
*.tgz

# ---------------------------------------------------------------------------
# 本地调试产物
# ---------------------------------------------------------------------------
# --patch 调试通道用的临时 overlay 目录（README「调试：两条通道，别混用」）。
.verify/
# npm 缓存被指到工作区内，用来绕开 D:\SDE\nodejs\node_cache 的 EPERM。
.npm-cache/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---------------------------------------------------------------------------
# 编辑器 / 系统
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini
.idea/
# 想共享 .vscode/settings.json 就把下面这行删掉。
.vscode/
*.swp
*~
